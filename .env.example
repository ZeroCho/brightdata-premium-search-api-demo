# Bright Data SERP API credentials
# Create a SERP API zone in Bright Data, then paste the API key and zone name here.
BRIGHT_DATA_API_KEY=your_bright_data_api_key
BRIGHT_DATA_SERP_ZONE=serp_api1

# Optional: set to fixture only for offline screenshots or tests.
# Leave unset in production. The live Vercel demo must not use fixture-only mode.
DEMO_MODE=
